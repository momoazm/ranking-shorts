# Secrets — the single shared credential file for every WAT project in this repo.
# NEVER commit this; it holds all API keys + Gmail config.
API.env

# Any stray per-project env files
**/.env

# Google OAuth
**/credentials.json
**/token.json
**/client_secret_*.json
GWS/

# Python / runtime artifacts
**/.venv/
**/__pycache__/
**/*.pyc
**/.tmp/

# Root env + assistant local files
.env
CLAUDE.local.md
.claude/settings.local.json
node_modules/

# Project MCP config (contains the 21st.dev key) — keep local
.mcp.json

# Obsidian workspace cache (per-machine UI state, constant churn)
Momo/.obsidian/
