# Secrets — NEVER commit. Reconstructed at runtime from GitHub Actions Secrets.
API.env
.env
credentials.json
token.json
*token*.json
cookies.txt

# Scratch / intermediates
.tmp/
exports/
__pycache__/
*.pyc
.venv/

# Persistent run state (used-clip history) — kept across runs via Actions cache, not committed
state/
