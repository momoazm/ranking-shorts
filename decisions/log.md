# Decision Log

Append-only. When a meaningful decision is made, log it here.

Format: [YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...

---

[2026-06-23] DECISION: Replaced the `competitor` WAT project with an inline skill `compare-youtube-channels`, and archived the project to `archives/competitor/`. | REASONING: The full PDF/email pipeline was overkill for what's actually wanted — a decision-ready, in-chat comparison of MOMO vs rival YouTube channels. An inline skill keeps it lightweight, leans on existing tools (newsletter's toolset) and the Firecrawl→Tavily→Exa research chain, and avoids a whole project + venv. Also corrected the stale niche: the skill targets MOMO's real niche (faceless ranking/Top-N Shorts), not the old "AI cinematic" framing in the dead profile. | CONTEXT: User chose inline skill / MOMO-vs-competitors / in-chat output. Competitor's tools were duplicated in newsletter/, so nothing was lost. Repointed Gmail-token reuse hints in clipping-auto + ranking shorts from `competitor/token.json` to `newsletter/token.json`. Archived (not deleted) per the house "don't delete — archive" rule.

[2026-06-23] DECISION: Brand refresh — brighter gold (`#C9A96C`→`#FFD23F`) and way-darker navy (`#0B1622`→`#040810`); recolored `logo.png` + `brandguidelines.png` and made the logo background transparent. | REASONING: Moemen asked for a more vivid gold and a much darker blue, and to remove the logo's dark box (it showed as a black rectangle on dark backgrounds, e.g. the infographic footer). | CONTEXT: Updated master `brand/theme.json` (colors + button/icon gold refs) and re-synced all project `brand/` copies. Logo + guidelines recolored via additive gold/navy anchor shifts (preserving shading); logo bg flood-filled to transparent. Originals archived in `archives/brand-old/`.

[2026-06-23] DECISION: Ranking Shorts hardened for virality — 1-min hard cap (was 2 min), intro swoosh removed, and three competitor-driven additions: a cold-open teaser, kinetic rank reveals, and curiosity-gap topic titles. Mirrored to the deployed repo momoazm/ranking-shorts (commit 92161d3). | REASONING: User asked for ≤1-min videos + no intro swoosh, plus a competitor comparison (compare-youtube-channels skill) that found MOMO's biggest gaps vs FailArmy/AFV/format-twins are (1) no upfront payoff promise, (2) static reveals, (3) flat topic framing. Fixes target exactly those: teaser flashes the #1 clip with "WAIT FOR #1" before #5; active leaderboard row pops 122%→100% on each reveal; rank_topic.py now writes curiosity-gap/payoff titles. | CONTEXT: Edited projects/ranking shorts/tools/{build_ranking_video.py,rank_autopost.py,rank_topic.py} + ranking-shorts.md. Verified end-to-end with synthetic clips (renders clean, ≤60s, teaser/board/#1 frames correct). The deployed repo's sync_from_source.py points at a stale "ai videos/" layout, so edited the GitHub copy directly per prior learning. Teaser is on by default (--no-teaser to disable); --intro-swoosh now off unless an explicit path is passed.

[2026-06-24] DECISION: clipping-auto clip length cut from 60s target/120s max to 35s target/60s cap (config/channels.json). | REASONING: Clips now auto-publish as YouTube Shorts; completion rate is the #1 Shorts ranking signal and peaks at 15-60s (OpusClip 2026 analysis of 13.5M+ clips), so 60-120s clips were suppressing reach. The earlier ≤2min preference was set for email-review delivery, a different context. | CONTEXT: Also rewrote reframe_crop.py to frame the ACTIVE SPEAKER (lip-motion score + hysteresis + segment-aware cuts) instead of the biggest/nearest face — fixed "wrong subject framed / pans through empty space." Upgraded select_clips.py hook prompt (first ~1.5s, curiosity-gap/stakes hooks). Aligned run_daily.py + select_clips/render_clip defaults + workflow docs to the 35/60 standard.

[2026-06-24] DECISION: Archived the `newsletter` project and relocated its Gmail email infra into `ranking shorts`. | REASONING: Moemen wanted the newsletter folder removed, but it held the only Gmail tooling + OAuth (used by the `send-email` + `infographics` skills and referenced by `compare-youtube-channels`). Deleting it outright would have broken email, which was just used to send an infographic. | CONTEXT: Moved `build_email_mime.py` / `send_gmail_email.py` / `gmail_auth_setup.py` → `projects/ranking shorts/tools/`, and the Gmail OAuth → `projects/ranking shorts/gmail/{credentials.json,token.json}` (subfolder so it reuses the `**/credentials.json`/`**/token.json` gitignore rules and doesn't collide with the YouTube `token.json`). Repointed `API.env` (`GMAIL_*_PATH=gmail/...`), the `send-email`/`infographics` skills, and the `compare-youtube-channels` prose. Verified mime build + token resolution from `ranking shorts`. Archived to `archives/newsletter/`. Also: brand gold re-tuned to richer #E6B23A (less lemon), and `build_infographic.py` gained `--icons` (one AI-generated icon per point).

[2026-06-29] DECISION: Merged the `compare-youtube-channels` skill and the `competitor-benchmark-sync` subagent into one new project, `projects/competitor-analysis/`, with a combined two-phase SOP (`workflows/competitor_analysis.md`: Instagram benchmarking + repo sync, then YouTube channel comparison) and a single entry point, the `/update` skill (no description, `context: fork` → `agent: update`). Also established the standing rule that subagents get no slash command and a short `description` by default — `update` is the one deliberate exception. | REASONING: User wanted both competitor-analysis capabilities consolidated into one callable workflow instead of two separate, narrower skills/agent, with the agent itself kept lean (slash-command default, description) but exempted as the one project the user explicitly wants slash-invocable. | CONTEXT: Deleted (not archived — explicit user choice, overriding the house archive rule for these two specific files) `.claude/skills/compare-youtube-channels/` and the old `.claude/skills/competitor-benchmark-sync/` wrapper. Renamed the subagent file `competitor-benchmark-sync.md` → `update.md` (frontmatter `name: update`); its body now just points at the new workflow SOP instead of duplicating it, removing ~130 lines of mistakenly-inlined auto-memory boilerplate in the process. Moved `momo-profile.json` (MOMO's YouTube baseline) into the new project. Updated root `CLAUDE.md`, `skill-builder.md`, and `agent-builder.md` to match.

[2026-06-29] DECISION: Renamed the `competitor-analysis` project's entry-point skill from `/update` to `/improver` (folder `.claude/skills/update/` → `.claude/skills/improver/`; `agent: update` link unchanged). | REASONING: User reported `/update` didn't trigger. No documented built-in `/update` command was found in Anthropic's commands reference (checked to rule out a name collision), so the more likely cause is that the underlying subagent file was hand-edited on disk this same session (renamed from `competitor-benchmark-sync.md` to `update.md`) — subagents load at session start and don't live-reload like skills do, unlike the skill wrapper itself. Renaming sidesteps the open question either way. | CONTEXT: Verified `/improver` (invoked via the Skill tool) actually forks and runs the full two-phase workflow end-to-end — it correctly found no `competitor/` folder (skipped Phase 1 per the workflow's own "don't fabricate" rule rather than improvising), and completed Phase 2 (YouTube comparison), updating `projects/competitor-analysis/momo-profile.json` with cited competitor data and writing agent memory. Updated all `/update` references in root `CLAUDE.md`, `skill-builder.md`, `agent-builder.md`, and the `competitor-analysis` project's own `README.md`/`CLAUDE.md`/workflow doc to `/improver`. The `update` subagent's `name` field is unchanged (still `update`) — only the slash command changed.

[2026-06-29] DECISION: Phase 2 (YouTube) of the `competitor-analysis` workflow now auto-commits `momo-profile.json` changes (no gate), but still does not push. Phase 1 (Instagram) keeps auto-commit + auto-push, unchanged. | REASONING: User asked whether `/improver` pushes to GitHub on every run, then chose to make Phase 2 consistent with Phase 1's no-gate commit behavior rather than adding a confirmation gate to either phase or leaving Phase 2 gate-less-but-uncommitted. Phase 2 still doesn't push because `momo-profile.json` isn't read by any deploy/automation — there's no "needs to be live now" reason to push immediately, unlike Phase 1's repo-config changes. | CONTEXT: Added step 13 to `projects/competitor-analysis/workflows/competitor_analysis.md` (renumbering the old step 13 "report back" to 14) and updated the project's `CLAUDE.md` (Phase 2 description + Hard rules) to state the asymmetric commit/push behavior explicitly.

[2026-06-29] DECISION: Added an automatic `PostToolUse` agent hook (`.claude/settings.json`) that ingests every new decision-log entry into the `Momo/` Obsidian vault. | REASONING: User wants all CLAUDE CODE folder changes "linked" into Momo, but the vault's own design is explicitly token-frugal and relationship-first (not exhaustive) — hooking every file write would flood it with noise. Scoped the hook to fire only on edits to `decisions/log.md` (matcher `Edit|Write`, `if: Edit(decisions/log.md)` / `Write(decisions/log.md)`), since that file already captures every "meaningful decision," making it the natural funnel for the vault. User explicitly confirmed this scope over "every change." | CONTEXT: Hook type is `agent` (model: sonnet) since the ingest needs judgment — read the latest log entry, skip if trivial, else create a small `sNNN` source node, link touched entity/concept nodes, update `index.md`, append to `log.md`, per `Momo/CLAUDE.md`'s own Ingest operation. First test (made before reloading via `/hooks`) didn't fire because the session had already cached the hooks config — reloaded via `/hooks`, retesting with this line. Confirmed general PostToolUse hooks now fire (sentinel test); this edit retests the `if`-scoped agent hook specifically.

[2026-06-29] DECISION: Abandoned the `PostToolUse` agent-type hook for Momo ingest; replaced it with a standing behavioral rule instead. | REASONING: The `agent`-type hook never fired across multiple `/hooks` reloads, with or without an `if` filter — confirmed via a sibling `command`-type hook in the same matcher array, which fired reliably every time. A `claude-code-guide` lookup found `"type": "agent"` is marked experimental and appears designed for yes/no PreToolUse gating decisions, not independent side-effecting tasks like a vault ingest. Given the choice between (a) a hook-based fix via shelling out to a headless `claude -p` subprocess with skipped permissions (a real unattended trust escalation) or (b) just doing the ingest myself in the same turn I append a decision, the user picked the simpler, already-reliable option. | CONTEXT: Reverted `.claude/settings.json` to `{}` (no hooks). Removed the now-unused experimental hook entries and test sentinel files. Added a standing rule to root `CLAUDE.md`'s Decision Log section: whenever a decision is appended here, immediately do the Momo ingest in the same turn, per `Momo/CLAUDE.md`'s Ingest operation — no hook plumbing involved.

[2026-06-29] DECISION: Enabled the experimental Agent Teams feature (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` in `.claude/settings.local.json`) and wrote a reference guide (`.claude/docs/agent-teams.md`), but added a standing rule in root `CLAUDE.md` that teams must never be auto-proposed or auto-spawned — only used when Moemen explicitly asks. | REASONING: Moemen wants the capability available for future use but doesn't want it firing automatically just because a task looks parallelizable; agent teams cost significantly more tokens than a subagent and add coordination overhead, so the default across every project here stays a single session or subagent. | CONTEXT: Created `.claude/agent teams/` (empty, user-requested) and `.claude/docs/agent-teams.md` (condensed from the upstream docs page: when to use, architecture, task sizing, best practices, troubleshooting, limitations). Added a new paragraph to CLAUDE.md's "Skills & Agents" section pointing at the guide and stating the explicit-request-only rule.

[2026-06-29] DECISION: Removed the `/improver` skill wrapper (`.claude/skills/improver/SKILL.md` deleted); the `competitor-analysis` project's `update` subagent now has no slash entry point and reverts to the platform default — invoke via natural language or `@agent-update`. | REASONING: Moemen asked to remove the improver skill directly. This also resolves the standing inconsistency where `update` was documented as the one "deliberate exception" to the no-slash-wrapper-by-default rule for subagents; removing it makes `update` follow the same default as every other subagent. | CONTEXT: Updated all references across root `CLAUDE.md` ("Skills & Agents" section, Projects line), `projects/competitor-analysis/README.md` and `CLAUDE.md` (Agents & invocation), `workflows/competitor_analysis.md` intro, and `.claude/skill-builder/{skill-builder.md,agent-builder.md}`'s "Current Capabilities" lists. The `update` subagent itself (`.claude/agents/update.md`) and its workflow SOP are unchanged — only the slash trigger is gone.

[2026-07-01] DECISION: Made the `Momo/` Obsidian vault the workspace's standing relationship-map with two always-on rules in root `CLAUDE.md` (new "Momo brain" section): (1) consult `Momo/index.md` + `[[links]]` first to learn how things relate / where they live before grepping the repo (token-saving); (2) ingest EVERY meaningful addition — new project, skill, subagent, feature, site, integration, or decision — into the vault the same turn, not just decision-log entries (which the prior rule alone covered). Also backfilled the previously un-noded pieces: skills `infographics` + `send-email`, the `cs-rag` multimodal RAG/vector-DB system (`projects/website/CS/`), and the `momo-website` "Knowledge Oracle" site (+ My Calendar feature); and ingested 2026-06-30's website work (PURE BLEND 3D site + frontend-design/video-to-website skills + 21st.dev rule) as source s006. | REASONING: Moemen asked to "add everything in Claude Code to Obsidian," "make sure everything is always added," and "make sure Claude Code always uses Obsidian to find relations and save tokens." The vault was only being fed decision-log entries, so anything built directly (the whole website/CS stack, the inline skills) never made it in; and the vault wasn't being used as a navigation aid at all. Generalizing both directions makes the brain actually serve its token-frugal, relationship-first purpose. | CONTEXT: Added the "Momo brain" section to root `CLAUDE.md` and folded the old decision-only ingest bullet into it; mirrored the convention into `Momo/CLAUDE.md` Defaults. Kept it a standing behavioral rule, not a hook — the `PostToolUse` agent hook was already tried and abandoned (2026-06-29). New wiki nodes across this session: sources s006 + s007; entities pureblend, frontend-design-skill, video-to-website-skill, cs-rag, momo-website, infographics-skill, send-email-skill; concept 21st-dev-magic. Vault is now current with the repo.

[2026-07-01] DECISION: Replaced the cron schedules on both self-hosted pipelines (clipping-auto `clipping_daily.yml` `0 21 * * *`, ranking-shorts `autopost.yml` `0 */4 * * *`) with on-demand triggering from password-gated "Run my pipelines" animated-badge buttons on the MOMO site (`projects/website/CS/index.html`). A new Vercel serverless function `api/runner.py` holds a GitHub fine-grained PAT and fires `workflow_dispatch`, gated server-side so a new run is refused while one is active; a tiny always-on local helper `runner-helper/runner_helper.py` polls GitHub and launches `run.cmd --once` for the matching runner so it starts -> runs one job -> exits. | REASONING: Moemen wanted each pipeline fired from a site button instead of cron, with the self-hosted runner alive only during a job and the button blocked until the current run finishes. The runners are his own PC (Reddit/YouTube IP-block GitHub-hosted runners), and a Vercel button can't reach a NAT'd home PC, so GitHub itself is the queue: the site dispatches a run, the local helper watches and starts the runner on demand, and `--once` makes the runner self-close. A token can't live in a static page, so it sits in the serverless function (Vercel env) behind a shared admin password, mirroring the existing `api/gcal.py`. Gate chosen = blocked only while a run is active (no time cooldown). | CONTEXT: Removed the `schedule:` blocks (kept `workflow_dispatch`). New files: `projects/website/CS/api/runner.py`; `runner-helper/{runner_helper.py,helper.env.example,start-helper.ps1,README.md,.gitignore}`; a "Run my pipelines" section in `index.html` with a hand-built animated conic-gradient badge restyled to MOMO gold from 21st.dev be-ui-animated-badge (@starc007). Verified read-only: both YAMLs parse to dispatch-only, the runs endpoint returns the fields parsed (status/conclusion/event/created_at), default branches are `main`, and the badge idle/running/disabled + locked states screenshot correctly. PENDING Moemen's go (irreversible/public): create the PAT (Actions RW on both repos), set Vercel env `GH_DISPATCH_TOKEN` + `RUN_ADMIN_PASSWORD`, drop the token into `runner-helper/helper.env` and register the logon task, then commit+push the workflow edits and deploy the site. No live run was triggered (would burn API credits / auto-post a video).

[2026-07-01] DECISION: Added a third World Cup angle — `streamer` — to the ranking-shorts engine (iShowSpeed / FaZe / Marlon / xQc etc. at the 2026 World Cup), sourced from YouTube search via a new `find_streamer_clips.py` tool rather than Reddit. `rank_autopost.py` now randomizes across three angles (fan/match share the football Reddit pool; streamer has its own YouTube pool) so each gets a fair share, with graceful fallback (streamer→Reddit streamer subs→mixed→fails). Angle wired through `rank_topic.py` (schema+lock), `rank_clips.py` (strict football/WC-only classifier), and `find_ranking_clips.py` (`--angle streamer` Reddit fallback subs). | REASONING: Moemen asked to clip famous streamers at the World Cup. A live RSS test proved Reddit is a poor source for this: r/LivestreamFail is drama/assault-heavy and mostly non-WC, and creator subs (r/ishowspeed) are low-volume with uninformative titles the title-based classifier can't use. YouTube search ("iShowSpeed World Cup football") returns on-theme, descriptively-titled clips the classifier CAN screen — validated: ~6–8 clean streamer-football-reaction clips per run, comfortably clearing the ≥5 gate. YouTube is viable again because the pipeline now runs on a self-hosted home-IP runner (cloud IPs are YouTube-bot-checked, the original reason for Reddit-only). Moemen accepted the higher Content-ID risk of streamer footage (mitigation stays the pitch-shifted music bed). | CONTEXT: New file `tools/find_streamer_clips.py` (flat yt-dlp search, keeps only known-short clips ≤240s so a VOD can't slip into the whole-file downloader; editable STREAMER_QUERIES roster; "adrima lima" from the roster left out pending correct spelling — a generic streamer query covers unnamed creators). Edited `tools/{rank_autopost,rank_topic,rank_clips,find_ranking_clips}.py` + `ranking-shorts.md`. All compile; source layer tested live (free search, no paid calls, no download/build/upload). NOT committed/pushed and no full paid dry-run yet — pending Moemen's go (this is the standalone repo momoazm/ranking-shorts; editing here = editing the deployed self-hosted pipeline).
